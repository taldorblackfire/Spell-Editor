@model IEnumerable<Spell_Editor.Models.Essence_Shaping>

@{
    ViewBag.Title = "Essence Shapings";
}

<h2>Essence Shapings</h2>

<p>
    @Html.ActionLink("Create New Essence Shaping", "Create")
</p>
<table id="ShapingTable" class="webgrid-table">
    <thead>
        <tr class="webgrid-header">
            <th>Name</th>
            <th>Effect</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="webgrid-row-style">
                <td>@item.Name</td>
                <td>@item.Effect</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script type="text/javascript">
        var table = $('#ShapingTable').DataTable();
    </script>
}
